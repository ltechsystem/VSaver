<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <!-- Ship as VSaver.exe (matches the GitHub repo and the auto-update release asset name). -->
    <AssemblyName>VSaver</AssemblyName>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <BuiltInComInteropSupport>true</BuiltInComInteropSupport>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <ApplicationIcon>app.ico</ApplicationIcon>
    <AvaloniaUseCompiledBindingsByDefault>true</AvaloniaUseCompiledBindingsByDefault>

    <!-- Bump this for every release. The tag on the GitHub Release must match (e.g. v1.0.1).
         The running build compares its version against the latest release to decide whether
         to auto-update. -->
    <Version>2.0.0</Version>
  </PropertyGroup>

  <ItemGroup>
    <!-- App/window icon (also embedded as the exe's Win32 icon above). -->
    <AvaloniaResource Include="app.ico" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Avalonia" Version="11.1.4" />
    <PackageReference Include="Avalonia.Desktop" Version="11.1.4" />
    <PackageReference Include="Avalonia.Themes.Fluent" Version="11.1.4" />
    <PackageReference Include="Avalonia.Fonts.Inter" Version="11.1.4" />
    <PackageReference Include="CommunityToolkit.Mvvm" Version="8.3.2" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\ValheimSync.Core\ValheimSync.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Let the test project reach internal helpers (e.g. NormalizeFolderId). -->
    <InternalsVisibleTo Include="ValheimSync.Tests" />
  </ItemGroup>

  <ItemGroup>
    <!-- Ship credentials.json next to the exe if present -->
    <None Update="credentials.json" Condition="Exists('credentials.json')">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>

</Project>
